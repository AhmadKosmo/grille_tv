<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Grille Tv", root_path, class: "navbar-brand", id: "logo"%>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Programmes", programmes_path%></li>
        <li><%= link_to "Réalisateurs", realisateurs_path%></li>
        <li><%= link_to "Chaines", chaines_path%></li>
        <li><%= link_to "Diffusions", diffusions_path%></li>
      </ul>
      <form class="navbar-form navbar-right">
        <%= form_tag(programmes_path, :method => "get", id: "search-form") do %>
        <%= text_field_tag :search, params[:search], class: 'form-control' %>
        <%= submit_tag "Search", class: 'btn btn-default' %>
        <% end %>
      </form>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
